[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "aurora-pnt"
version = "1.0.0"
description = "AURORA PNT — LEO satellite navigation and timing system by Shiwa Network. Combined LEO+GLONASS positioning, sovereign LPT time scale, global coverage."
readme = "README.md"
requires-python = ">=3.8"
license = {file = "LICENSE"}
authors = [
  {name = "Shiwa Network", email = "carolinechambersk@proton.me"}
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: GNU Affero General Public License v3",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering",
]
dynamic = ["dependencies"]

[project.scripts]
aurora-pnt = "aurora.pnt.cli:main"

[project.urls]
"Homepage" = "https://github.com/ShiwaNetwork/aurora-pnt"

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}

[tool.setuptools.packages.find]
where = ["."]
include = ["aurora*"]

[tool.setuptools.package-data]
"aurora" = ["**/*.yaml", "**/*.html", "**/*.js", "**/*.css", "**/*.png"]

[tool.mypy]
ignore_missing_imports = true

[tool.coverage.run]
source = ["livecov"]
omit = [
    "aurora/tests/*",
    "aurora/__init__.py",
    "aurora/logger.py",
    "aurora/satellite_visualisation/*",
]

[tool.pytest.ini_options]
addopts = "--cov-report xml:cov.xml --cov aurora"
testpaths = ["tests"]
log_cli = true
log_level = "INFO"
log_cli_format = "%(asctime)s [%(levelname)8s] %(message)s (%(filename)s:%(lineno)s)"
log_cli_date_format = "%Y-%m-%d %H:%M:%S"

[tool.black]
line-length = 100

[tool.isort]
profile = "black"
line_length = 100
multi_line_output = 3
include_trailing_comma = true
force_grid_wrap = 0
use_parentheses = true
ensure_newline_before_comments = true
